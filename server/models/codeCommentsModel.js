class CodeComment {
    constructor(id, codeId, userId, comment) {
      this.id = id;
      this.codeId = codeId;
      this.userId = userId;
      this.comment = comment;
    }
  }
  
  module.exports = CodeComment;
  